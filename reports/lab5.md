# 1. 简单总结你实现的功能（200字以内，不要贴代码）。
1. 合并 sys_get_time
    > a. 使能时间获取。
2. 实现 sys_enable_deadlock_detect
    > a. 可通过参数打开/关闭死锁检测选项。死锁检测的方法为分别对 mutex 与 semaphore 的资源分配使用受限 banker’s algorithm （具体思路为假设当前次分配完成后分配矩阵 ALLOCATION 在数值上与最大需求矩阵 MAX 相等， MAX 矩阵的具体数值随每一次分配不断变化）。

# 2. 完成问答题。
1. 主线程退出时的资源回收问题
    1) 属于该进程的所有资源（如地址空间，文件描述表，任务资源分配器， mutex 表， semphore 表等 ProcessControlBlock中包含的资源，以及各个线程的 TaskControlBlock中的资源）。
    2) 可能在 manager 队列和 TIMER 堆中，需要回收。
2. Mutex1 等待的线程被唤醒后会再判断一次锁的状态,而Mutex2没有。因为是 add_task() ，所以被唤醒的线程不一定会马上被调度,导致 Mutex2 恢复运行时会直接访问临界区,而此时可能有其他线程也正在访问临界区。

# 3. 加入荣誉准则的内容。否则，你的提交将视作无效，本次实验的成绩将按“0”分计。
1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

（无）

此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

关于对代码中 banker's algorithm 的限制，刚开始并明确，在查阅了大量的资料，使用 AI 工具生成参考思路，以及参考了 GitHub 用户 numpy1314 的思路后明确了添加限制的具体要求，但具体代码仍然靠自己独立完成。

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。

# 4. (optional) 你对本次实验设计及难度/工作量的看法，以及有哪些需要改进的地方，欢迎畅所欲言。
ch8 的实验设计有点太宽泛了， banker's algorith 的限制没有明确的界限，自己得不停的去试各种策略去找到一种能通过所有测例的方案（一种策略是预设 NEED，不断地去凑数值，还有就是像此处的解答一样设计 MAX 在每次资源分配/释放中不断变化的情况），但这样的一个方案又不一定具备普遍性，移植到其他使用场景中可能效果就很差。